# 라이브러리
- 정상 제어를 하는 모듈
 
- 정상 제어 : 모듈의 기능을 개발자가 직접 호출하는 것 
```python
import math
math.sin()
math.cos()
math.tan()
```
이렇게 개발자가 직접호출을 했다. 
이런 것을 정상제어라고 한다. 

# 프레임워크 
제어 역전을 하는 모듈 
제어 역전 : 모듈이 개발자가 만들 기능을 호출하는 것이다. 
```python
@app.route("/")
def hello()
    return "<h1>안녕하세요</h1>"
```
여기서 개발자는 본인이 만든 hello라는 함수를 직접 호출하진 않는다.모듈에서 hello 함수를 호출해서 직접 사용한다. 이렇게 제어 역전이 발생하는 모듈을 프레임워크라고 부른다. 

# 라이브러리와 프레임워크의 구별
이러한 측면에서 beautifulsoup 모듈은 라이브러리라고 부를 수 있고, flask는 프레임워크라고 부를 수 있다. 